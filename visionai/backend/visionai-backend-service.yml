apiVersion: v1
kind: Service
metadata:
  name: visionai-backend-service
spec:
  selector:
    app: visionai-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP